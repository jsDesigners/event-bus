{
  "name": "@jsdesigners/event-bus",
  "description": "The ES6-ready EventBus class for managing events in JavaScript",
  "scripts": {
    "build": "rollup -c",
    "watch": "rollup -c --watch",
    "test": "ava"
  },
  "devDependencies": {
    "ava": "^2.2.0",
    "rollup": "^1.19.0",
    "rollup-plugin-terser": "^5.1.0"
  }
}
